class User_upload {
  constructor(pointSlideID, humidity, temperature, thermalPerformance, thermalSensation, username, airQuality, utcDateTime, roomNumber) {
    this.pointSlideID = pointSlideID;
    this.humidity = humidity;
    this.temperature = temperature;
    this.thermalPerformance = thermalPerformance;
    this.thermalSensation = thermalSensation;
    this.username = username;
    this.airQuality = airQuality;
    this.utcDateTime = utcDateTime;
    this.roomNumber = roomNumber;
  }
}
